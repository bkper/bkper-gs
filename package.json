{
  "name": "@bkper/bkper-app",
  "version": "0.0.11",
  "description": "Google Apps Script library for Bkper",
  "homepage": "https://bkper.com/api",
  "repository": {
    "type": "git",
    "url": "https://github.com/bkper/bkper-app.git"
  },
  "bugs": {
    "url": "https://github.com/bkper/bkper-app/issues",
    "email": "support@bkper.com"
  },
  "author": {
    "name": "bkper, Inc",
    "url": "https://bkper.com"
  },
  "scripts": {
    "clean": "rimraf build",
    "open": "clasp open",
    "types:generate": "yarn clasp-types",
    "types:publish": "yarn --cwd dist/@bkper/bkper-app-types publish --access public --new-version $npm_package_version",
    "test": "run-s test:*",
    "test:bundle": "tsc --skipLibCheck --sourcemap --outFile build/test-bundle.js --module system",
    "test:run": "mocha --require source-map-support/register build/test-bundle.js",
    "push": "clasp push",
    "preversion": "run-s test push",
    "postversion": "yarn types:generate && yarn types:publish && git push && git push --tags && echo \"Successfully released version $npm_package_version!\""
  },
  "devDependencies": {
    "@google/clasp": "^2.3.0",
    "@types/chai": "^4.2.3",
    "@types/mocha": "^5.2.7",
    "@types/node": "^12.7.5",
    "chai": "^4.2.0",
    "clasp-types": "^1.1.4",
    "copyfiles": "^2.1.1",
    "mocha": "^6.2.0",
    "npm-run-all": "^4.1.5",
    "rimraf": "^3.0.0",
    "source-map-support": "^0.5.13",
    "typedoc": "^0.15.0",
    "watch": "^1.0.2"
  },
  "dependencies": {
    "@types/google-apps-script": "^0.0.59"
  }
}
