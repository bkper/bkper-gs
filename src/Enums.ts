

  enum Periodicity {

    DAILY = "DAILY",
    MONTHLY = "MONTHLY",
    YARLY = "YARLY"
  }

  enum DecimalSeparator {
    COMMA = "COMMA",
    DOT = "DOT"
  }

  enum Permission {
    NONE = "NONE",
    VIEWER = "VIEWER",
    RECORD_ONLY = "RECORD_ONLY",
    POST = "POST",
    EDITOR = "EDITOR",
    OWNER = "OWNER"
  }

  enum BalanceType {
    TOTAL = "TOTAL",
    PERIOD = "PERIOD",
    CUMULATIVE = "CUMULATIVE"
  }